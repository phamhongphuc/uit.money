<?xml version="1.0" encoding="utf-8"?>
<layout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:context=".activity.LoginActivity">

    <data>

        <variable
            name="adapter"
            type="uit.money.fragment.FragmentAdapter"/>
    </data>

    <ui.Col
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="#fff"
        android:gravity="center">

        <ui.FrameShadow
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:layout_marginLeft="25dp"
            android:layout_marginRight="25dp"
            android:layout_marginTop="25dp"
            android:layout_weight="1"
            android:background="#2ecc71"
            app:_radius="@dimen/radius"
            app:_shadowColor="#bb2ecc71"
            app:_shadowSize="@dimen/shadow_size">

            <android.support.v4.view.ViewPager
                android:id="@+id/viewPagerLogin"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:adapter="@{adapter}"
                android:layerType="hardware"/>

        </ui.FrameShadow>

        <ui.Button
            android:layout_width="match_parent"
            android:layout_height="@dimen/element_height_with_shadow"
            android:layout_margin="25dp"
            android:background="#3498db"
            android:onClick="login"
            app:_backgroundIcon="#2000"
            app:_childRadius="none"
            app:_font="regular"
            app:_foreground="#fff"
            app:_icon=""
            app:_radius="@dimen/radius"
            app:_rippleColor="#99ffffff"
            app:_shadowColor="#884cb7ff"
            app:_shadowSize="@dimen/shadow_size"
            app:_text="@string/login_with_facebook_button"
            app:_textAlign="left"/>
    </ui.Col>
</layout>
